import 'package:flutter/material.dart';

class ComponentAddingOverlay extends StatelessWidget {
  const ComponentAddingOverlay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
