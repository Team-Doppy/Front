import 'package:flutter/material.dart';

class PostviewScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
          child: Text("Postview Screen"),
        )
    );
  }
}
